# Backend environment example

# Port the Express server listens on
PORT=3000

# JWT secret for signing auth cookies
JWT_SECRET=change-me

# Postgres connection (used by Sequelize)
# If using docker-compose, the host is 'db'
DATABASE_URL=postgres://campusx:campusx@localhost:5432/campusx

# CORS allowed origin for cookies in dev
CORS_ORIGIN=http://localhost:5174